# Initialization

The function **eCLD\_IdentifyCreateIdentify\(\)** is used to create an instance of the Identify cluster. This function is called by the initialization function for the host device but can alternatively be used directly by the application in setting up a custom endpoint which supports the Identify cluster \(among others\).

**Parent topic:**[Identify Cluster](../../Identify_cluster/topics/identify_cluster.md)

