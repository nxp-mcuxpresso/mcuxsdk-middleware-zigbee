# Application link key modification

An application link key can be introduced or replaced by the application using **zps\_eAplZdoAddReplaceLinkKey\(\)**. If a link key already exists for the same node- pair, it will be replaced by the new link key. The function must be called on both nodes in the pair.

**Parent topic:**[Security key modification](../topics/security_key_modification.md)

